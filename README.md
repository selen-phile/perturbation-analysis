This repository contains the code and experimental results for analyzing the internal effects of query perturbations in large language models.
The code covers:

Layer-wise representation drift
Feature activation changes
Attention-head sensitivity analysis

The results/ directory contains the corresponding CSV files generated from the experiments.

Reproducibility

The experiments consider multiple perturbation types, datasets, and language models. Configuration details and paths required to reproduce the analyses are provided within the respective scripts.
